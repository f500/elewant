{% extends '@HWIOAuth/layout.html.twig' %}

{% block hwi_oauth_content %}

    <h3>{{ 'header.success' | trans({}, 'UserBundle') }}</h3>
    <p>{{ 'paragraph.connecting' | trans({'%name%': app.user.displayName, '%service%': ('service.' ~ service) | trans({}, 'UserBundle')}, 'UserBundle') | nl2br }}</p>
    <p><a href="{{ path('herd_tending') }}">{{ 'paragraph.herd_tending' | trans({}, 'UserBundle') }}</a></p>

{% endblock hwi_oauth_content %}
